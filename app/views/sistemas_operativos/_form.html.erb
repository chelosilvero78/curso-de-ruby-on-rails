<%= form_for(@sistema_operativo) do |f| %>
  <% if @sistema_operativo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sistema_operativo.errors.count, "error") %> prohibited this sistema_operativo from being saved:</h2>

      <ul>
      <% @sistema_operativo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
