<%= form_for(@accesorio) do |f| %>
  <% if @accesorio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accesorio.errors.count, "error") %> prohibited this accesorio from being saved:</h2>

      <ul>
      <% @accesorio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modelo_id %><br />
    <%= f.collection_select :modelo_id, Modelo.all, :id, :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
