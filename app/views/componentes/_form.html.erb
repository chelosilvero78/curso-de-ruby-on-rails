<%= form_for(@componente) do |f| %>
  <% if @componente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@componente.errors.count, "error") %> prohibited this componente from being saved:</h2>

      <ul>
      <% @componente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :proveedor_id %><br />
    <%= f.collection_select :proveedor_id, Proveedor.all, :id, :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
