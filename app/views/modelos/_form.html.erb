<%= form_for(@modelo) do |f| %>
  <% if @modelo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modelo.errors.count, "error") %> prohibited this modelo from being saved:</h2>

      <ul>
      <% @modelo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :serie_id %><br />
    <%= f.collection_select :serie_id, Serie.all, :id, :nombre %>
  </div>
  <div class="field">
    <%= f.label :sistema_operativo_id %><br />
    <%= f.collection_select :sistema_operativo_id, SistemaOperativo.all, :id, :nombre %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :fecha_lanzamiento %><br />
    <%= f.date_select :fecha_lanzamiento %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
